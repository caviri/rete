{
  "title": "Gate Card Fixture",
  "description": "A fixture card, written at build time so the gate can read a real one. Holds a <script> tag and an & on purpose: card text is third-party data and must never reach the DOM as live markup.",
  "license": "CC0-1.0",
  "source": "https://example.org/gate-fixture"
}
