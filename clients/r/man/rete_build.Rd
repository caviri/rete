% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/build.R
\name{rete_build}
\alias{rete_build}
\title{Build a .rete file image from RDF text}
\usage{
rete_build(
  text,
  format = "nt",
  card = NULL,
  pyramid = c("louvain", "types", "none"),
  text_index = FALSE
)
}
\arguments{
\item{text}{RDF text: N-Triples (\code{"nt"}), N-Quads (\code{"nq"} — named graphs
become a dataset), Turtle (\code{"ttl"}), or RDF/XML (\code{"rdfxml"}).}

\item{format}{The RDF serialization of \code{text}.}

\item{card}{Optional Dataset Card as a named list (curated fields such as
\code{title}, \code{description}, \code{license}, \code{created}, \code{example_queries}; counts
are stamped automatically). Read it back with \code{\link[=rete_card]{rete_card()}}.}

\item{pyramid}{Community pyramid algorithm: \code{"louvain"} (topological,
default), \code{"types"} (one community per \code{rdf:type} class), or \code{"none"}.}

\item{text_index}{If \code{TRUE}, add the opt-in full-text word index that
powers \code{\link[=rete_text_search]{rete_text_search()}}.}
}
\value{
A raw vector holding the file image — pass it to \code{\link[=rete_open]{rete_open()}} or
write it with \code{writeBin()}.
}
\description{
Assembles a complete, immutable \code{.rete} file — dictionary, triple indexes,
pyramid, and optionally an embedded Dataset Card — from RDF text. Handy
for tests and small graphs; for large datasets use the \verb{rete build} CLI,
which streams and compresses.
}
\examples{
nt <- '<urn:a> <urn:knows> <urn:b> .'
g <- rete_open(rete_build(nt, card = list(title = "Tiny demo")))
rete_query(g, "SELECT ?o WHERE { <urn:a> <urn:knows> ?o }")
}
