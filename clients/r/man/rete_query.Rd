% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/query.R
\name{rete_query}
\alias{rete_query}
\title{Run a SPARQL query}
\usage{
rete_query(graph, query, reason = FALSE)
}
\arguments{
\item{graph}{A \code{rete_graph} from \code{\link[=rete_open]{rete_open()}}.}

\item{query}{A SPARQL query string.}

\item{reason}{If \code{TRUE}, answers include OWL 2 QL entailment, computed by
query rewriting over the file's ontology (works on remote files too).}
}
\value{
A \code{data.frame}, or a logical scalar for ASK.
}
\description{
Evaluates a SPARQL query against the graph. SELECT queries return a
\code{data.frame} with one column per variable — IRI brackets are stripped and
typed literals are coerced (\code{xsd:integer} to integer/double,
\code{xsd:double}/\code{decimal}/\code{float} to double, \code{xsd:boolean} to logical); other
terms come back as character. ASK returns a logical scalar;
CONSTRUCT/DESCRIBE return a \code{data.frame} with \code{subject}, \code{predicate},
\code{object} columns. Use \code{\link[=rete_query_raw]{rete_query_raw()}} for the unprocessed result
envelope with full term fidelity.
}
