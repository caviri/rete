% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/open.R
\name{rete_open}
\alias{rete_open}
\title{Open a .rete graph}
\usage{
rete_open(source)
}
\arguments{
\item{source}{A file path, an \verb{http(s)://} URL, or a raw vector.}
}
\value{
A \code{rete_graph} object.
}
\description{
Opens a \code{.rete} file for SPARQL querying: a local path, an \verb{http(s)://}
URL, or a raw vector holding a complete file image. Local and remote opens
are lazy — remote files are read over HTTP range requests, fetching only
the byte ranges each query touches (the host must honor \code{Range}).
}
\examples{
\dontrun{
g <- rete_open("https://data.graphplaza.com/boe/boe.rete")
rete_query(g, "SELECT ?s ?p ?o WHERE { ?s ?p ?o } LIMIT 5")
}
}
