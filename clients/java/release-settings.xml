<?xml version="1.0" encoding="UTF-8"?>
<!--
  Maven settings for publishing to the Sonatype Central Portal. Contains NO
  secrets — credentials are read from environment variables at build time (set
  them from .env; see RELEASING.md). Safe to commit.

  Use with:  mvn -s clients/java/release-settings.xml -Prelease ...
-->
<settings xmlns="http://maven.apache.org/SETTINGS/1.0.0"
          xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
          xsi:schemaLocation="http://maven.apache.org/SETTINGS/1.0.0 https://maven.apache.org/xsd/settings-1.0.0.xsd">
  <servers>
    <server>
      <id>central</id>
      <username>${env.MAVEN_USERNAME}</username>
      <password>${env.MAVEN_PASSWORD}</password>
    </server>
  </servers>
</settings>
